import React from 'react';
import { Card, Icon } from 'antd';

export default (props) => {
  return (
    <Card>
      <div>
        <Icon type="user" />
      </div>
      <div>
        Number Card
      </div>
    </Card>
  );
}